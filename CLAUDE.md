# App Dev Team 週會逐字稿 AI 輔助流程 — 專案交接說明

> 這份文件是給 Claude Code（或任何接手這個專案的 Claude session）看的完整背景，
> 重點是記錄計劃書定案過程中每個決策的「為什麼」，讓你不用重新問一輪需求就能延續同一套邏輯。
> **實作現況、技術棧、已知限制以 [`README.md`](./README.md) 為準**——那份文件會隨程式碼更新，這份不會，只留決策脈絡。

---

## 專案目標（一句話）

每週五 App Dev Team 開實體 weekly meeting，只有音檔（無錄影）。
需要一套流程，把音檔轉成**繁體中文逐字稿草稿**，用 AI 輔助降低人工聽打負擔，
**而非追求特定準確率數字**——這點很重要，見下方「關鍵決策」。

目前狀態（2026-08-11 確認過一次，之後請以 README.md 為準）：**計劃書（docx）已定案，Phase 1 自動化腳本已實作完成**（`scripts/asr_pipeline/`：多軌對齊、降噪正規化、混音、語者分段、faster-whisper 轉錄、簡轉繁），Phase 2（依 RD 報告切檔具名命名）尚未實作。AI 校對因使用者僅有 Claude Team 帳號、無 API 計費權限，改為在 Claude Code 對話中人工貼草稿校對，不是獨立腳本。

---

## 已交付的檔案

| 檔案 | 說明 |
|---|---|
| `App_Dev_Team_逐字稿自動化計劃書.docx` | 完整計劃書，Word 格式，可直接給主管/團隊看 |
| `scripts/build_plan_docx.js` | 產生上述 docx 的 Node.js 腳本（用 `docx` npm 套件），若要再改計劃書內容，改這支腳本後重新跑即可 |
| `README.md` | 技術現況說明，給接手維護者/貢獻者看，實作進度與已知限制以此為準 |
| `scripts/asr_pipeline/` | Phase 1 實作：`setup.py`、`preprocess.py`、`align_mix.py`、`diarize.py`、`transcribe.py`、`phase1_pipeline.py` 等，細節見 README.md |

---

## 關鍵決策與背景（依討論順序，含「為什麼」）

這些決策都是在對話中一步步被要求／修正出來的，**每一項都有明確理由，不要在沒問過使用者的狀況下改回去**。

### 1. 錄音環境
- 實體會議，**只有音檔，沒有錄影**。
- 錄音裝置：手機 + MacBook Pro (MBP) 內建麥克風，**可能擴充到 2–4 台裝置**（多機多軌）。
- 使用者強調過一次：不是固定「雙機」，術語要用「多機／多軌」，邏輯要能吃 2、3、4 台裝置。

### 2. 隱私要求：絕對不能上雲、不能用 OpenAI API
- 使用者原話：「我不想把資料給 OpenAI」。
- **重要澄清**：Whisper「模型」本身是開源的，可以完全本機執行，不等於 OpenAI 的付費 API。
  - 選用方案：`faster-whisper` 執行 `large-v3` 模型，**在本機或內部主機／VM 跑，音檔全程不出公司環境**。
  - 只有「逐字稿文字」（不是音檔）會傳給 Claude 做校對這一步，這是唯一接觸外部服務的環節。
- 執行環境：**現有 i7 筆電／i7 PC Server，不足時請 MIS 開內部 VM**。**明確不用 GPU**，純 CPU 執行是接受的前提。
- 因為沒有 GPU，運算可能明顯慢於即時，因應方式是「用時間換」：**排程在週五深夜到週末，於 VM 上跑批次**，目標是**下週一上班前**產出草稿（不是會議結束後 24 小時內，這兩者在週五開會的情境下會衝突，已修正過)。

### 3. 多機多軌整合邏輯
- 各軌**先各自跑本機 ASR**，保留時間戳記。
- 自動時間對齊：用音訊互相關（cross-correlation），**不需要開會前拍手對時**。
- 比對合併邏輯：
  - **3 軌以上**：多數決。
  - **僅 2 軌**：沒有第三方可投票，改用「兩軌一致則採用，不一致則標記待確認」。
- **重要限制（使用者要求誠實揭露，不能省略）**：
  - 多軌比對只能標出「意見不一致」的地方，不代表沒被標記的就一定對。
  - 若多台裝置擺位相近（例如都在會議室同一側），收音特性接近，**錯誤可能相關**，多軌比對會失去意義——因應對策是裝置分散擺放於不同側。
- 曾經寫過「全軌同時辨識錯誤機率 < 20%」這個統計目標，**後來被使用者要求整個拿掉**（見第 5 點）。

### 4. 音訊前處理
- **音量正規化（loudness normalization）**：用 `ffmpeg` 的 `loudnorm` 濾鏡，EBU R128 標準。
  - **這不是憑空猜測**：使用者親口說「人工做過三週的音量增益，對辨識有明顯幫助」，所以這步驟是把已驗證有效的人工調整自動化，不是空想的假設。
- **降噪**：目標**明確限縮為「抑制冷氣等穩定背景噪音」**，不追求處理所有雜訊類型。
  - 人聲重疊、會議室回音——降噪演算法對這類雜訊效果有限，**這個限制已寫進風險表，不要嘗試用軟體解決，優先靠麥克風擺位與發言秩序預防**。
- 音檔格式：**統一轉成 m4a**（原本是 mp3/wav，使用者最後一次要求改成 m4a，命名規則例如 `2026-08-14_AppDevWeekly.m4a`）。

### 5. 「準確率」框架被整個拿掉（非常重要，不要加回去）
這是整個對話裡最大的一次轉向，**背景值得完整記錄**：

1. 一開始計劃書設定「99% 字詞正確率」為目標。
2. 使用者要求我（Claude）「grill」這個計劃，逐條質疑：
   - 99% 用什麼標準量測？WER 還是 CER？
   - 抽查只看關鍵段落，沒被抽到的部分憑什麼說是 99%？
   - 本機跑 Whisper large-v3 沒有 GPU，運算時間撐得住嗎？
   - 多軌「多數決」在只有 2 軌時邏輯根本不成立？
   - 對照表誰維護？沒人是正式 owner 通常第 3 週就停擺？
3. 使用者針對每一點給出具體修正方向（見上面第 1–4 節），計劃書因此補了大量「誠實揭露限制」的內容。
4. 我後續又自行二次審查（self-review），抓出更多邏輯漏洞（例如「24 小時目標」跟「週末不上班」互相打架），也修正了。
5. 使用者再問我一次：**「現在計劃書中的所有措施，有辦法將錯誤率拉到 99% 嗎？」**
   - 我誠實回答：**不行**。自動化那段（ASR+AI校對）的準確率本身沒有驗證，人工抽查只看局部，多軌 <20% 只在特定條件下有效，而真正扛住 99% 這個數字的其實是「開放修訂」這個沒有品管機制的環節——沒人規定誰要看、看多少、沉默不代表正確。
6. 使用者的結論：**乾脆把所有跟準確率相關的數值跟說明全部拿掉，不要著重在準確率上，單純當 AI 輔助就好。**
7. 我因此把整份計劃書重寫一次，**移除所有數字性的準確率宣稱**（99%、WER、85–95%、98–99%、<20% 全軌錯誤機率），改成：
   - 逐字稿明確定位為「AI 輔助產出的草稿」，不是保證品質的定稿。
   - 品質把關機制是「開放修訂」：草稿廣發後，全體團隊成員可以訂正，彙整後才是定稿。
   - 風險表新增一條：「草稿被團隊當成定稿直接引用，沒人做開放修訂」——因應對策是廣發時明確標示「草稿，待確認」字樣，並定期提醒團隊參與訂正，**不能預設沉默等於正確**。
   - 成功指標從「準確率數字」改成「流程本身是否可行、可重複、對團隊是實際的負擔減輕」——草稿時效、定稿時效、流程穩定性、人工投入時間。

**如果接下來要寫自動化腳本，這個決策直接影響設計：不要在程式裡刻意去逼近某個準確率數字，重點是把流程跑順、把「待確認清單」和「開放修訂」的介面做好。**

### 6. 人工投入時間要誠實
- 使用者明講：「人工下來做這些事情絕對超過三、五個小時」。
- 計劃書因此**拿掉了原本不切實際的「15 分鐘」「5-10 分鐘」估計**，改成「初期（第 1–4 週）每次 3–5 小時，隨對照表與流程成熟逐步下降」。

---

## 目前流程（九步驟，計劃書 4.1 節）

1. 錄音品質把關（會議桌中央放手機／全向麥克風，錄音前測試 30 秒）
2. 音檔轉存整理（統一轉 **m4a**，固定命名規則，存內部資料夾）
3. 音訊前處理（音量正規化 + 降噪，鎖定冷氣噪音）
4. ASR 初稿轉錄（**faster-whisper + large-v3，本機／VM 執行，不上雲**）
5. 專有名詞比對（套用團隊維護的對照表；多機時同步做多軌交叉比對）
6. AI 二次校對（Claude 依對照表校正錯字、標點、講者標示）
7. 人工快速抽查（關鍵段落 + 多軌分歧段落）
8. 草稿產出與分發（**目標下週一上班前**，廣發並標示「草稿，待確認」，開放全員訂正）
9. 彙整修訂形成定稿（收集回饋，更新對照表供下週使用）

多機音軌方案（進階選配，計劃書第五章）銜接在步驟 4 與 5 之間，取代單軌轉錄。

---

## 角色分工（計劃書第七章）

| 角色 | 負責事項 |
|---|---|
| 會議主持人／紀錄窗口 | 啟動錄音、確認收音品質、音檔存入指定資料夾 |
| 流程執行人 | 執行前處理、ASR 轉錄、AI 校對，產出草稿 |
| 內容把關人 | 人工抽查關鍵段落與多軌分歧段落 |
| 對照表維護人（**需指定備援**，單點故障是列在風險表的已知風險） | 定期更新專有名詞對照表 |
| 全體團隊成員 | 草稿廣發後協助訂正，回饋給流程執行人彙整定稿 |

---

## 尚未完成的下一步

Phase 1 腳本已實作並在一台機器上實測跑過一次（見 README.md「實測效能數據」）。尚未完成的部分：

- **Phase 2**：依 RD 報告內容切成個別檔案並具名命名（`yyyyMMdd_SerialNumber_RDName.*`），目前完全沒開始。
- **多機環境驗證**：候選環境還有 Ubuntu Server（i7-7700＋32GB）、Windows 10 筆電（i5-8250U＋32GB），`setup.py` 邏輯上支援但尚未實測跑通。
- **斷錄接續邏輯的真實案例校準**：`align_mix.py` 的 `CONFIDENCE_THRESHOLD` 只用「確實重疊」案例校準過，還沒有真正的裝置中途斷錄案例驗證。
- 排程批次執行（週五深夜到週末跑，週一上班前出草稿）目前是手動跑 `phase1_pipeline.py`，還沒接上 cron / Windows 工作排程器。

技術棧、檔案結構、已知限制、實測效能數據等細節一律以 README.md 為準，不在這裡重複維護。

---

## 使用者溝通風格備註

- 使用者會針對計劃書的每個環節深挖細節（例如追問「多機可以到三、四機嗎」「音軌增益怎麼處理」），傾向給出具體、可執行的規格，而不是含糊的方向。
- 使用者重視誠實揭露限制，不喜歡「聽起來很棒但沒有實測支撐」的宣稱（例如要求把 AI 校對 98-99% 這種未經驗證的數字標註為「預估值」）。
- 使用者會主動要求「grill」/「review」/「驗證」計劃內容，預期得到不留情面的批判性檢查，而不是安慰式的附和。
